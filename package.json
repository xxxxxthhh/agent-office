{
  "name": "agent-office",
  "version": "0.4.0",
  "description": "A local-first workflow control plane for Codex, Claude Code, Herdr, and shell tools.",
  "type": "module",
  "bin": {
    "agent-office": "./bin/agent-office.js"
  },
  "files": [
    "bin/",
    "src/",
    "dashboard/",
    "schemas/",
    "docs/",
    "examples/",
    "README.md",
    "LICENSE",
    "tools/",
    "tests/"
  ],
  "scripts": {
    "check": "node --check bin/agent-office.js && node --check src/capabilities.js && node --check src/config.js && node --check src/orchestrator.js && node --check src/workflow-definition.js && node --check src/workflow-orchestrator.js && node --check src/execution-runtimes.js && node --check src/workspaces.js && node --check src/server.js && node --check dashboard/app.js && node --check tools/run-tests.mjs && node --check tools/_process-ledger.cjs && node tools/run-tests.mjs",
    "test": "node tools/run-tests.mjs",
    "demo": "node ./bin/agent-office.js demo"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
